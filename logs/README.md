# logs/

Local working artifacts only — not tracked by git.

- `claude/` — session transcripts from Claude Code prompt-chain runs (`.log` files, excluded by `.gitignore`)
- `codex/` — session transcripts from Codex runs (if applicable)

These files exist to aid review and debugging of AI-assisted sessions. They are not part of the build and should not be committed.
